# 项目技术梳理初稿

> 本文为 e-voice 系列项目的第一次技术梳理，用于明确现有功能模块、依赖、配置、文档及目录结构，并标注各项资产的使用状态。后续删除或迁移前需由负责人确认，并做好 Git 备份。

## 1. 项目总览

| 子项目 | 目录 | 角色定位 | 核心技术栈 | 状态 |
| --- | --- | --- | --- | --- |
| 实时语音识别后端 | `e-voice/` | Flask + SocketIO 服务，负责语音识别、声纹注册、会议转写等核心 AI 能力 | Python 3.12、ModelScope、Torch、FunASR、Elasticsearch、MySQL | 在用 |
| 业务网关 / 管理后台后端 | `e-voice-admin/` | Go 服务，封装管理端 API、网关转发、角色权限、离线会议数据管理等 | Go 1.23、Gin、GORM、Redis、PostgreSQL/MySQL | 在用 |
| 管理后台前端 | `e-voice-admin-front/` | Vue 3 + Arco Design 中台，提供声纹管理、会议管理、系统配置等 UI | Vite、Vue 3、Pinia、Axios、Socket.IO client | 在用 |
| 文档归档 | `e-docs/` | 迁移出的历史/技术文档与提示词库 | Markdown、HTML | 在用（文档存档） |
| 历史备份 | `e-old/` | 旧版 `rest.py` 单文件备份 | Python | 疑似无用（待确认是否保留历史对比） |

## 2. 功能模块梳理

### 2.1 Python 服务（`e-voice/`）

| 模块 | 说明 | 关键依赖/接口 | 状态 |
| --- | --- | --- | --- |
| `rest.py` | SocketIO + Flask 入口，加载 `server` 工厂并启动服务，支持健康检查、监控开关等 | `server.create_app`、`PORT` 环境变量 | 在用 |
| `server/` | 模块化的 REST/WebSocket 实现，按蓝图划分基础接口、语音注册/识别、热词管理、日志查询、调试、状态查询等；`session.py` 实现输入法风格流式会话；`text_processing.py` 负责热词替换/指令检测 | Flask、Flask-Sock、Flask-SocketIO、NumPy、SoundFile、Pydub、ModelScope | 在用 |
| `rest_prints.py` | 声纹注册/识别 API，调用 Elasticsearch 向量检索、语音识别与中文纠错 | `es.voice`、`pipeline.spk_v_pipeline`、`speech_recognition.recognize`、`zh_correct.correct` | 在用 |
| `rest_meeting.py` | 会议离线转写接口，接收上传音频后异步提交 `biz.meeting.process_audio_task`，拆分说话人并入库 | ThreadPoolExecutor、Pydub、SoundFile、Torch | 在用 |
| `speech_recognition/` | ModelScope 语音识别与词汇管理封装：`recognize.py` 主识别流程（含兜底逻辑）、`vocabulary_manager.py` 自定义词汇加载、`spk.py` 多说话人识别、`speech_recognition_streaming.py` 流式识别配置 | ModelScope pipeline、FunASR、PyCorrector | 在用 |
| `pipeline/spk_v_pipeline.py` | 说话人向量化封装，生成声纹 embedding | ModelScope `speaker-verification` | 在用 |
| `biz/meeting/` | 离线会议业务逻辑，负责说话人聚合、音频切片、批量写入会议明细 | FunASR AutoModel、Elasticsearch、Pydub | 在用 |
| `db/` | MySQL 连接与 `meeting_offline_detail` 域模型封装 | PyMySQL、Loguru | 在用 |
| `es/` | Elasticsearch 客户端与声纹索引管理，提供索引建表、增删改查、向量检索 | `elasticsearch` SDK | 在用 |
| `monitoring/` | 系统监控模块（CPU/GPU/识别指标），通过 `server.monitoring` 可选启用 | `psutil`、Torch、Loguru | 在用（可选） |
| `error_handling/` | 错误恢复管理器，提供重试、降级、组件恢复策略 | Loguru、Enum | 在用（可选增强） |
| `zh_correct/` | 中文纠错及词频资源，`correct.py` 初始化 `pycorrector`，`custom_word_freq*.txt` 为热词表 | PyCorrector | 在用 |
| `tests/` | 手工测试与调试工具：Shell/API 页面、WebSocket/性能/验证脚本（均 `pytest.skip` 防止自动运行）、浏览器测试页面组件 | curl、pytest、websocket-client、前端静态页 | 在用（手工验证） |
| `scripts` | `install.sh`、`start_rest.sh`、`restart_rest.sh`、`finetune.sh` 等环境/部署脚本 | Conda、Gunicorn、Torchrun | 在用（运维脚本） |
| `resource/` | 示例音频、架构图与数据样本 | WAV/MP3 | 在用（测试数据） |
| `ModelScope深度调试方案.py` 等 | ModelScope 深度调试、临时方案脚本 | ModelScope、Torch | 疑似无用（调试脚本，待确认是否保留） |
| `librosa-test/`、`speech_recognition/gpu-test/`、`biz/meeting/time_test.py` | 音频可视化、GPU 环境检测、小型时间测试脚本 | Librosa、Torch | 疑似无用（便捷脚本，未在主流程引用） |
| `test_speech_recognition.py` | 结合日志输出的调试脚本，默认 `pytest.skip` | Flask、pytest | 在用（手工调试） |

### 2.2 Go 网关（`e-voice-admin/`）

| 模块 | 说明 | 关键依赖 | 状态 |
| --- | --- | --- | --- |
| `main.go` & `internal/route` | 初始化配置、IOC、Gin 路由与 OpenAPI 静态资源，监听端口 & 优雅停机 | Gin、自研 `ioc`/`openapi` 模块 | 在用 |
| `internal/app/voice/print.go` | 对接 Python 声纹服务的 CRUD/API 转发，封装用户声纹管理与鉴定 | `config.Inst.App.Micro['py_voice']`、HTTP 客户端 | 在用 |
| `internal/app/meeting/offline.go` | 离线会议任务管理、文件上传转发、状态查询 | HTTP 客户端、业务服务层 | 在用 |
| `internal/api/*` | OpenAPI 文档注册（声纹、会议） | `openapi.GroupDoc` | 在用 |
| `internal/domain` & `internal/model` | GORM 实体、服务、DTO 定义 | GORM、JSON | 在用 |
| `pkg/` | 通用组件（日志、IOC、工具、OpenAPI 静态资源等） | Logrus/自研 | 在用 |
| `dev_tools/` | 数据库迁移、代码生成、DB 测试工具 | GORM、Go CLI | 在用（开发工具） |
| `resource/config*.yml` | 多环境配置（数据库、Redis、微服务地址、JWT、开放接口白名单等） | YAML | 在用 |
| `test/set_default_test.go` | 结构体默认值测试 | `structx` | 疑似无用（示例测试，需确认） |

### 2.3 前端管理端（`e-voice-admin-front/`）

| 模块 | 说明 | 关键依赖 | 状态 |
| --- | --- | --- | --- |
| `src/views/voice/print` | 声纹注册、实时录音、批量导入、鉴定等页面及 API 封装 | Arco 组件、`defHttp`、文件上传 | 在用 |
| `src/views/meeting/offline` | 离线会议列表、详情、音频上传、标注编辑 | Arco 表格/表单、HTTP | 在用 |
| `src/views/voice/identify` | 在线/离线识别前端页，调用 Python 服务实时/批量识别 | Socket.IO client、HTTP | 在用 |
| `src/store/modules` | `app`、`tab-bar`、`user` Pinia 模块 | Pinia | 在用 |
| `src/utils/http` | Axios 封装、重试、取消、状态码处理 | Axios、TypeScript | 在用 |
| `vite.config.ts` | 构建/开发配置，代理 `/api` `/resource` 到 Go 服务，启用代码检查与按需打包 | Vite、插件体系 | 在用 |
| `config/settings.json` | 主题 & 布局默认值 | JSON | 在用 |
| `node_modules/` | 依赖安装产物 | - | 在用 |
| `.env*` | 默认空；README 建议使用 `.env.local` 配置编辑器路径 | - | 在用（需自建） |

### 2.4 文档与资料

| 路径 | 说明 | 状态 |
| --- | --- | --- |
| `e-docs/e-voice/实时语音识别系统完整技术文档.md` 等 | 历史架构、调试记录、API 说明 | 在用（参考资料） |
| `e-docs/提示词/202509/` | 历史提示词与重构说明 | 在用（背景信息） |
| `e-voice/tests/README.md` | 测试工具目录说明 | 在用 |

## 3. 依赖 & 外部服务

### 3.1 Python (`e-voice`)
- 依赖清单见 `requirements.txt`，核心包括 Flask 3.1、Flask-SocketIO、Eventlet、ModelScope 1.22、FunASR 1.2.6、Torch 2.7、Elasticsearch 8.12、PyMySQL、psutil 等。
- 需要本地或远程：MySQL、Elasticsearch（支持 dense_vector）、可选 GPU/CUDA 环境。

### 3.2 Go (`e-voice-admin`)
- `go.mod` 包含 Gin、GORM、JWT、Redis、FFmpeg-Go、Go-Cache 等常见依赖。
- 默认配置使用 PostgreSQL (`driver: postgres`)，亦提供 MySQL 驱动；Redis 用于缓存。

### 3.3 前端 (`e-voice-admin-front`)
- `package.json` 依赖 Vue 3 生态、Arco Design、Axios、Pinia、Socket.IO client、ECharts 等。
- 使用 Vite 构建，`pnpm` 推荐管理依赖，代理指向 Go 服务端口 8108。

## 4. 重要配置

| 文件 | 作用 | 说明 |
| --- | --- | --- |
| `e-voice/config/config.py` | 读取 `EVOICE_ENV` 环境变量并加载对应 `.ini` | 默认为 `dev`，需根据部署创建 `prod.ini` 等 |
| `e-voice/config/*.ini` | 数据库、ES、文件路径配置 | `voice.temp_path`、`meeting.offline_wav_path` 等需指向可写目录 |
| `e-voice-admin/resource/config*.yml` | Go 服务环境配置 | `micro.py_voice.host` 指向 Python 服务（默认 `http://127.0.0.1:8210`） |
| `e-voice-admin-front/.env.local`（建议） | 前端本地配置 | README 建议配置编辑器路径，代理由 `vite.config.ts` 控制 |
| `start_rest.sh` / `restart_rest.sh` | Python 服务部署脚本 | 使用 Conda、Gunicorn 启动，默认 `EVOICE_ENV=prod` |

## 5. 接口与测试

- **REST 接口**：
  - `/voice-register`、`/voice-recognize-offline`、`/voice-recognize-online`（声纹 & 语音识别）。
  - `/meeting/offline` 上传会议音频并异步处理。
  - `/prints/*` 声纹列表、删除、鉴定。
  - `/logs/*`、`/api/hotwords/*`、`/ws/status` 等运维接口。
- **WebSocket**：`/ws/recognize` 实现输入法风格实时识别（含指令识别、热词纠错、重复过滤）。
- **测试工具**：
  - `tests/test_api.sh`：Shell 脚本验证健康检查、注册、CORS、错误处理。
  - `tests/pages/`：HTML 前端工具页（语音注册、实时识别、Embedding 测试）。
  - `tests/websocket_test.py`、`validation_test.py`、`performance_test.py`：手动运行的 pytest 脚本。
  - 前端通过 `pnpm run dev` + Proxy 指向 Go 服务调试。
  - Go 侧提供 `dev_tools/migrate`、`dev_tools/dbtest` 进行数据库校验。

## 6. 代码目录结构（关键路径）

```
├─ e-voice/
│  ├─ rest.py / rest_meeting.py / rest_prints.py
│  ├─ server/
│  │   ├─ routes/{basic,voice,ws,hotwords,debug,logs,status}.py
│  │   ├─ session.py / audio_utils.py / text_processing.py / monitoring.py
│  ├─ speech_recognition/ / pipeline/ / biz/ / db/ / es/
│  ├─ monitoring/ / error_handling/ / zh_correct/
│  ├─ tests/ / resource/ / scripts
│  └─ ModelScope调试脚本、librosa-test 等（待确认）
├─ e-voice-admin/
│  ├─ main.go / internal/{app,api,config,domain,route}
│  ├─ pkg/{logx,ioc,utils,...}
│  ├─ resource/{config*.yml,webadmin,webbusiness}
│  └─ dev_tools/ / test/
├─ e-voice-admin-front/
│  ├─ src/{views,api,store,utils,layout,router}
│  ├─ config/settings.json / vite.config.ts / tsconfig.json
│  └─ public/ / types/ / node_modules/
├─ e-docs/
│  ├─ e-voice/{完整技术文档、调试记录、API 文档等}
│  └─ 提示词/202509/ 重构说明
└─ e-old/rest.py.bak （旧版备份）
```

## 7. 疑似无用 & 待确认清单

| 路径 | 说明 | 建议 |
| --- | --- | --- |
| `e-old/rest.py.bak` | 旧版 3000 行 `rest.py` 备份 | 确认是否仍需保留历史对照；若无需求可备份后删除 |
| `e-voice/ModelScope深度调试方案.py`、`简化调试方案.py` | 临时调试脚本，未在服务入口引用 | 若无人使用可迁移到 docs 或删除 |
| `e-voice/librosa-test/` | 音频可视化示例，仅用于分析 | 迁入 `e-docs` 或移除 |
| `e-voice/speech_recognition/gpu-test/torch-test.py` | GPU 环境检测脚本 | 确认是否仍需；可改放到运维脚本目录 |
| `e-voice/biz/meeting/time_test.py` | Python 时间计算小脚本 | 若无用可删除 |
| `e-voice-admin/test/set_default_test.go` | 结构体默认值示例测试 | 若无持续价值可移除 |

> 所有“疑似无用”项须在负责人确认后再执行删除，删除前请打 Tag 或创建备份分支。

## 8. 后续建议

1. **配置收敛**：梳理 `config/*.ini` 与 `resource/config*.yml` 的本地路径（如个人目录路径）并统一为相对/环境变量配置，便于部署。
2. **测试自动化**：将 `tests` 中的手工脚本整理为可配置的 pytest/CI 流程，避免遗漏。
3. **调试脚本归档**：将 ModelScope 调试方案、librosa 示例移入 `e-docs` 或专用 `tools/` 目录，减少生产代码噪音。
4. **依赖升级检查**：Python 端 Torch/FunASR 版本较高，需验证 GPU 环境兼容性；Go 端确认 PostgreSQL 与 MySQL 的实际使用场景。

---

以上为初稿，欢迎在审核时补充缺失模块或纠正状态标记，确认后可据此执行进一步清理与优化工作。
